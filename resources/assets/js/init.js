(function () {
    'use strict';
    $(document).ready(function () {
        switch($("body").data("page-id")) {
            case 'home':
                break;
            default:
                // Do nothing
        }
    })
})();
